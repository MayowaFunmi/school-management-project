// import { DecodedToken } from "./jwtUtils"

//  const hasAdminRole = (decodedToken: DecodedToken): boolean => {
//     return !!decodedToken.roles.find((role) => role.roleName === 'ADMIN');
// };
export {}